import React from 'react';

// LanguageSwitcher has been removed as the application is now English-only
// This is a placeholder component to avoid import errors
export default function LanguageSwitcher() {
  return null;
}