// Empty mock data file - all mock data has been removed
import type { MissionOrder } from './MissionOrderTypes';

export const generateMockMissionOrders = (): MissionOrder[] => {
  // No mock data - all missions should come from the database
  return [];
};